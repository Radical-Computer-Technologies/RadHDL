library ieee;
use ieee.std_logic_1164.all;
use ieee.numeric_std.all;

-- Generated by scripts/generate_radhdl_api.py. Do not hand-edit.
package RadCDC is
  -- Component declaration sourced from common/hdl/src/radhdl_cdc.vhd.
  component rad_cdc_single is
    generic (
      VENDOR : string := "generic";
      STAGES : positive := 2;
      INIT   : std_logic := '0'
    );
    port (
      dest_clk : in  std_logic;
      src_i    : in  std_logic;
      dest_o   : out std_logic
    );
  end component;

  -- Component declaration sourced from common/hdl/src/radhdl_cdc.vhd.
  component rad_cdc_array_single is
    generic (
      VENDOR     : string := "generic";
      DATA_WIDTH : positive := 1;
      STAGES     : positive := 2;
      INIT       : std_logic := '0'
    );
    port (
      dest_clk : in  std_logic;
      src_i    : in  std_logic_vector(DATA_WIDTH - 1 downto 0);
      dest_o   : out std_logic_vector(DATA_WIDTH - 1 downto 0)
    );
  end component;

  -- Component declaration sourced from common/hdl/src/radhdl_cdc.vhd.
  component rad_cdc_sync_rst is
    generic (
      VENDOR      : string := "generic";
      STAGES      : positive := 2;
      ACTIVE_HIGH : boolean := true
    );
    port (
      dest_clk : in  std_logic;
      rst_i    : in  std_logic;
      rst_o    : out std_logic
    );
  end component;

  -- Component declaration sourced from common/hdl/src/radhdl_cdc.vhd.
  component rad_cdc_async_rst is
    generic (
      VENDOR      : string := "generic";
      STAGES      : positive := 2;
      ACTIVE_HIGH : boolean := true
    );
    port (
      dest_clk : in  std_logic;
      rst_i    : in  std_logic;
      rst_o    : out std_logic
    );
  end component;

  -- Component declaration sourced from common/hdl/src/radhdl_cdc.vhd.
  component rad_cdc_pulse is
    generic (
      VENDOR : string := "generic";
      STAGES : positive := 2
    );
    port (
      src_clk      : in  std_logic;
      src_rst      : in  std_logic;
      src_pulse_i  : in  std_logic;
      dest_clk     : in  std_logic;
      dest_rst     : in  std_logic;
      dest_pulse_o : out std_logic
    );
  end component;

  -- Component declaration sourced from common/hdl/src/radhdl_cdc.vhd.
  component rad_cdc_gray is
    generic (
      VENDOR     : string := "generic";
      DATA_WIDTH : positive := 4;
      STAGES     : positive := 2
    );
    port (
      dest_clk    : in  std_logic;
      src_gray_i  : in  std_logic_vector(DATA_WIDTH - 1 downto 0);
      dest_gray_o : out std_logic_vector(DATA_WIDTH - 1 downto 0)
    );
  end component;

  -- Component declaration sourced from common/hdl/src/radhdl_cdc.vhd.
  component rad_cdc_handshake is
    generic (
      VENDOR     : string := "generic";
      DATA_WIDTH : positive := 1;
      STAGES     : positive := 2
    );
    port (
      src_clk     : in  std_logic;
      src_rst     : in  std_logic;
      src_valid_i : in  std_logic;
      src_data_i  : in  std_logic_vector(DATA_WIDTH - 1 downto 0);
      src_ready_o : out std_logic;
      dest_clk    : in  std_logic;
      dest_rst    : in  std_logic;
      dest_valid_o: out std_logic;
      dest_data_o : out std_logic_vector(DATA_WIDTH - 1 downto 0);
      dest_ready_i: in  std_logic
    );
  end component;

end package RadCDC;
